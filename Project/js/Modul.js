//加载模块
layui.use(['form', 'upload', 'element', 'carousel'], function () {  //如果只加载一个模块，可以不填数组。如：layui.use('form')
    var form = layui.form ;//获取表单模块
    var upload = layui.upload; //获取上传模块
    var element = layui.element; //获取element模块
    var carousel = layui.carousel;//获取轮播图模块
});
